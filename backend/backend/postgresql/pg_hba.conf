# TYPE  DATABASE        USER            ADDRESS                 METHOD
# IPv4 local connections - TRUST for Windows/psycopg3 compatibility
host    all             all             127.0.0.1/32            trust
# IPv6 local connections - TRUST for Windows/psycopg3 compatibility  
host    all             all             ::1/128                 trust
# Allow replication connections from localhost
host    replication     all             127.0.0.1/32            trust
host    replication     all             ::1/128                 trust
# Docker network connections - require password
host    all             all             172.16.0.0/12           scram-sha-256
host    all             all             192.168.0.0/16          scram-sha-256
# All other connections require password
host    all             all             all                     scram-sha-256
